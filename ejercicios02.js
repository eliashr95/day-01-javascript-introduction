//Escribir un algoritmo que determine si un número es par o impar.
function esPar(num) {
  if (num % 2 == 0) {
    console.log('su numero ingresado es par');
  } else {
    console.log('su numero no es par');
  }
}
esPar(24);

//Crear un algoritmo que determine el día de la semana
//según un número del 1 al 7.

let diaDeLaSemana = 'Martes';

switch (diaDeLaSemana) {
  case 'Lunes':
    1;
    break;
  case 'Martes':
    2;
    break;
}

console.log(diaDeLaSemana(1));
